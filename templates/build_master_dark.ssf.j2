requires 1.4.0
# Generated by app/ssf.py — do not hand-edit (regenerate via /masters/render).
# Runs as its own siril-cli process (Handoff.md gotcha #8) into a
# disposable scratch dir; app/main.py moves master_dark.fit to its stable
# location afterward (Handoff.md gotcha #6).
cd "{{ raw_dir }}"
convert dark -out={{ process_dir }}
cd "{{ process_dir }}"
stack dark {{ stack_cmd }} -nonorm -out=master_dark
close
