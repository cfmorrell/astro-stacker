requires 1.4.0
# Generated by app/ssf.py — do not hand-edit (regenerate via /stack/render).
# Single-night final step: register + stack that night's own pp_light
# sequence directly (no merge needed with only one night — see
# calibrate_night.ssf.j2's sibling step, which already produced it, and
# Handoff.md gotcha #7: Siril's `merge` refuses fewer than two inputs).
cd "{{ process_dir }}"
register pp_light{{ register_flags }}
stack r_pp_light {{ stack_cmd }} -norm=addscale -output_norm -out={{ output_name }}
close
